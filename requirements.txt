# openBIMForge Python Dependencies
#
# ⚠️  The authoritative dependency list now lives in pyproject.toml. This
# file is kept for legacy tooling (pip install -r) only. Prefer uv:
#
#     uv sync --extra dev    # installs runtime + pytest
#     uv run pytest tests    # runs the test suite
#
# If you edit this file, mirror the change in pyproject.toml.
-e .
